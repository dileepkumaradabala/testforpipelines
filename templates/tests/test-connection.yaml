apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "new-test.fullname" . }}-test-connection"
  labels:
    {{- include "new-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "new-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
